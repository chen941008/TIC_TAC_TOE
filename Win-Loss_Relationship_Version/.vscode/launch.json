{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/Win-Loss_Relationship_Version",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build",
            "console": "integratedTerminal",
            "sourceMap": {
                "${workspaceFolder}": "${workspaceFolder}"
            }
        }
    ]
}